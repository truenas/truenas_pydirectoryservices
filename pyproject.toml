[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "truenas-nss"
version = "0.1.0"
description = "CPython extension for direct NSS lookups (passwd/group)"
requires-python = ">=3.11"
authors = [{name = "TrueNAS Engineering", email = "dev@truenas.com"}]
license = {text = "LGPL-3.0-or-later"}
